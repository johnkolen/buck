<% transfer.comments.each do |comment| %>
  <hr>
  <%= render :partial=>"comments/comment", :locals=>{:comment=>comment}%>
<% end %>
<button class="btn btn-default btn-info" data-toggle="modal" data-target="#comment_modal">
  Add Comment
</button>
