<% transfer =
    render :partial=>"transfers/show",:locals=>{:transfer=>@comment.transfer} %>
content = $("<%=escape_javascript(transfer)%>")

id = content.attr("id");
$("#"+id).replaceWith(content);
$("#comment_modal").modal("hide");
