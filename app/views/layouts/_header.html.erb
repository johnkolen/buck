<div class=row>
  <div class="col-sm-8 col-sm-offset-2 col-md-4 col-md-offset-4">
    <div class=row>
      <div class="navbar navbar-default" role="navigation">
	<div class="container-fluid">
	  <div class="navbar-header">
	    <button type="button" class="navbar-toggle collapsed"
		    data-toggle="collapse"
		    data-target="#bs-example-navbar-collapse-1">
              <span class="sr-only">Toggle navigation</span>
              <span class="icon-bar"></span>
              <span class="icon-bar"></span>
              <span class="icon-bar"></span>
	    </button>
	    <a class="navbar-brand" href="#"><%=logo :small%></a>
	  </div>

	  <ul class="nav navbar-nav navbar-right">
	    <p class="navbar-text">
	      Hi <%=@current_user.first_name%>!
	    </p>
	    <% unless session[:pending] %>
	    <li class="dropdown">
	      <a href="#" class="dropdown-toggle" data-toggle="dropdown">
		Pages
	      </a>
	      <ul class="dropdown-menu" role="menu">
		<li><%=link_to "Profile", user_path(session[:user_id])%></li>
		<li><%=link_to "Dashboard",
	        dashboard_user_path(session[:user_id])%></li>
		<li><%=link_to "Recent", recent_transfers_path%></li>
		<% if session[:is_admin] %>
		  <li><%= admin_link %></li>
		<% end %>
	      </ul>
	    </li>
	    <% end %>
	    <li><%= header_logout_link%></li>
	  </ul>
	</div>
      </div>
    </div>
  </div>
</div>
